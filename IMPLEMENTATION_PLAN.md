# 三省六部制新闻 Agent 实施计划

## 目标

在保留现有新闻采集、去重、聚类、Claim 核验、简报、提醒与部署链路的基础上，加入可审计的中书省拟稿、门下省封驳、尚书省执行状态机，以及六部并行专责办理；同时以独立实现的宣纸、墨色、朱砂、裁角、印章和主从阅读窗格重构 Web UI。

## 架构决策

1. 不复制 `labs-ArchiveAssistant` 的 GPL 代码、字体或图片，只借鉴公开的信息架构和视觉概念。
2. 工作流由确定性规则驱动，AI 只参与经引用校验的文字生成，不拥有批准、驳回或分派权限。
3. 使用当前投影、运行记录、六部分派、六部 attempt 工作报告、追加式迁移审计五张表；数据库唯一约束与运行栅栏共同保证幂等和并发安全。
4. 中书省形成提案与部门分派；门下省检查独立来源、转载占比、来源身份、必需覆盖和 Claim 证据；尚书省先下执行令、再汇总六部具报，最后调用现有摘要与提醒服务。
5. 六部允许多值分派并真实生成报告：吏部负责来源身份；户部负责经济金融；礼部负责外交社会；兵部负责冲突安全；刑部负责法律调查与事实核查；工部负责科技、基础设施、气候与灾害。报告只转述已有 Claim 或原始材料，并保留文章级 Citation。
6. 保留全部现有 URL，并新增 `/workflow`、`/ministries/:id` 和事件工作流审计接口。
7. 首页宽屏采用案簿/奏折/批红三栏，中屏双栏，移动端单窗标签；六部页面只把后端工作报告呈现为“具报”。
8. Service Worker 导航使用 network-first 与 `no-store`，哈希静态资源使用 cache-first；构建为已知旧壳生成入口兼容别名，每个新 release 再于激活前以拒绝链接、严格文件名和 no-clobber 方式继承历史哈希 JS/CSS，兼容尚未更新 Service Worker 的旧 HTML 壳。

## 实施顺序

1. 扩展共享类型、常量、数据库 Schema 与迁移。
2. 实现纯规则分派与三省治理函数。
3. 实现 WorkflowStore、WorkflowService、容器和调度器接入。
4. 扩展公共/管理 API，并强化摘要与提醒幂等。
5. 新增三省总览、六部页面、事件详情审计视图和新设计系统。
6. 补齐单元、API、构建与浏览器测试。
7. 运行质量门、安全审查，部署到 `news.11451405.xyz` 并做公网验收。

## 验收标准

- `AI_PROVIDER=none` 时工作流仍完整运行。
- 同一证据输入重复调度不会产生重复运行、迁移或提醒。
- 每次迁移可查询规则版本、原因和结构化产物。
- 一个事件可以同时分派给多个部门。
- 六部每轮恰有六份终态工作单；获分派部门必须完成，未分派部门必须以 `blocked` 留痕。
- 尚书省只有在六部工作单、摘要与提醒全部成功后才可成报并设为可发布。
- 旧 run 或过期 attempt 不能覆盖当前案件投影、报告或执行令。
- 门下省封驳表示证据不足，不表示新闻为假。
- 新证据到达后可重新审议。
- 现有采集、事件、搜索、来源、问答和简报 API 保持兼容。
- 移动端和桌面端均可用，支持键盘、减少动画和高对比度。
- 桌面三栏、中屏双栏、移动单窗均无横向溢出；页面导航更新后不复用陈旧 HTML 壳。
- 历史缓存 HTML 引用的 Vite 哈希 JS/CSS 在新 release 首次打开时仍返回成功，新构建同名资源不被历史文件覆盖。
- 同步 SQLite 与规则分析在条目、事件和三省六部阶段边界主动让出事件循环；工作流健康聚合使用覆盖索引，后台批处理不会让健康检查长期饥饿或扫描大体积 JSON 投影。
